# Env & secrets
.env
*.local

# Go
/bin/
/server.exe
/server

# Air (hot reload dev di Docker) — dibuat via bind mount container, bukan kode
/tmp/

# Node / frontend
node_modules/
web/dist/
web/dev-dist/

# sqlc & tooling cache
.sqlc/

# OS / editor
Thumbs.db
.DS_Store
.idea/
.vscode/

# Uploads (data runtime, bukan kode)
/uploads/

# Backup database (data runtime, bukan kode — lihat scripts/backup.sh)
/backups/
